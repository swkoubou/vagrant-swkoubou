swkoubou.vagrant
================

vagrant

sahara
---------
スナップショットを取れる  
1. `vagrant plugin install sahara`  
2. `sed -i -e "s/VBOX_INSTALL_PATH/VBOX_MSI_INSTALL_PATH/g" C:/Users/user/.vagrant.d/gems/gems/sahara-0.0.16/lib/sahara/session/virtualbox.rb`: Windowsの場合のみ。バージョンやホームディレクトリのパスは適宜変更してください。

